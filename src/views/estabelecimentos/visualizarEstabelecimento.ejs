<!DOCTYPE html>
<html lang="pt-br">
<%- include('../partials/head', { title: 'Visualizar Estabelecimento' }) %>

<body class="bg-light">
    <%- include('../partials/header') %>

    <div class="header-spacer"></div>

    <div class="container w-75">
        <main class="mb-5">
            <div class="card shadow-sm border border-primary border-opacity-25 rounded-3">
                <div class="card-header bg-warning text-dark d-flex justify-content-between align-items-center">
                    <h5 class="mb-0"><i class="bi bi-eye-fill me-2"></i> Visualizar Estabelecimento</h5>
                    <a href="/estabelecimentos/table" class="btn btn-dark btn-sm">Voltar</a>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h4 class="card-title mb-0">Estabelecimento: <%= estabelecimento.estabelecimento %></h4>
                        <span class="badge bg-secondary">ID: <%= estabelecimento.id %></span>
                    </div>
                    <div class="row">
                        <!-- Coluna Esquerda -->
                        <div class="col-md-6">
                            <p><strong>Status:</strong> <%= estabelecimento.status.charAt(0).toUpperCase() + estabelecimento.status.slice(1).toLowerCase() %></p>
                            <p><strong>Produtos:</strong> <%= estabelecimento.produto %></p>
                            <p><strong>Responsável:</strong> <%= estabelecimento.responsavel_nome %></p>
                            <p><strong>Telefone:</strong> <%= estabelecimento.telefone_contato.replace(/(\d{2})(\d{5})(\d{4})/, '($1) $2-$3') %></p>
                            <p><strong>Dia do Cadastro:</strong> <%= new Date(estabelecimento.createdAt).toLocaleDateString('pt-BR') %></p>
                            <p><strong>Observações:</strong> <%= estabelecimento.observacoes || 'Nenhuma observação' %></p>
                        </div>
                        <!-- Coluna Direita -->
                        <div class="col-md-6">
                            <p><strong>Chave:</strong> <%= estabelecimento.chave || 'Não informada' %></p>
                            <p><strong>Máquina:</strong> <%= estabelecimento.maquina || 'Não informada' %></p>
                            <p><strong>Endereço:</strong> <%= estabelecimento.endereco %></p>
                            <p><strong>Bairro:</strong> <%= estabelecimento.bairro %></p>
                            <p><strong>Última Edição:</strong> <%= new Date(estabelecimento.updatedAt).toLocaleDateString('pt-BR') %></p>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <%- include('../partials/footer') %>
</body>
</html>